/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'www.notion.so', 'avatars.githubusercontent.com'],
},
};

export default nextConfig;
